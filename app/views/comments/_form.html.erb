<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		<%= f.label :İsim %><br>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.label :Yorum %><br>
		<%= f.text_area :body %>
	</p>
	<br>

	<p>
		<%= f.submit 'Gönder' %>
	</p>
<% end %>

