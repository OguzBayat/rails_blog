<div id="post_content">
	<h1 class="title"><%= @post.title %></h1>

	<p class="date">
	<% if user_signed_in? %>
	 <%= link_to 'Düzenle', edit_post_path(@post) %> | <%= link_to 'Sil', post_path(@post), method: :delete, data: { confirm: 'Emin misin?'} %></p>
	<% end %>

	<p class="body"><%= @post.body %></p>

	<div id="comments">
		<h2><%= @post.comments.count %> Yorum</h2>
		<%= render @post.comments %>

		<h2>Yorum Ekle:</h2>
		<%= render "comments/form" %>
	</div>
</div>